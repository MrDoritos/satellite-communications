#pragma once
#include <cmath>

class utils {
public:
static double degtorad(double degrees);
static double radtodeg(double radians);
static double wrap(double degrees, double max);
static double pi;
};
